extends layout

block content
  if user
    h1 WELCOME BACK #{user.username.toUpperCase()}
    a(href='/log-out') LOG OUT
  else
    h1 Please log in or sign up
    if messages.error
      p.error= messages.error
    include user/login
    h1 
      a(href='/signup') Sign Up